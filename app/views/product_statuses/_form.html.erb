<%= form_with(model: product_status, local: true) do |form| %>
  <% if product_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_status.errors.count, "error") %> prohibited this product_status from being saved:</h2>

      <ul>
        <% product_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :managable %>
    <%= form.number_field :managable %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
