<%= form_with(model: product_subtype, local: true) do |form| %>
  <% if product_subtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_subtype.errors.count, "error") %> prohibited this product_subtype from being saved:</h2>

      <ul>
        <% product_subtype.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :product_type_id %>
    <%= form.text_field :product_type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
